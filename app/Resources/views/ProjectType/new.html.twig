{% extends '::layout.html.twig' %}

{% block content %}
    <h1>ProjectType creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('config_project_type') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
