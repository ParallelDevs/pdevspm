{% extends '::layout.html.twig' %}

{% block page_title -%} Tickets List{% endblock %}

{% block content %}

    <div class="col-md-12">
        <div class="box box-primary">
            <div class="box-header">
                <i class="fa fa-filter"></i>
                <h3 class="box-title">Filters</h3>
            </div>
            <div class="box-body pad table-responsive">
                Here will go the filters
            </div><!-- /.box -->
        </div>
    </div>


    <div class="col-xs-12">
        <div class="box box-primary">
            <table class="table table-hover">
                <thead>
                   <tr>
                        <th>Id</th>
                        <th>Name</th>
                        <th>Description</th>
                        <th>Createdat</th>
                        <th>Actions</th>
                   </tr>
                </thead>
                <tbody>
                    {% for entity in entities %}
                        <tr>
                            <td><a href="{{ path('ticket_show', { 'ticket_id': entity.id, 'project_id': entity.project.id }) }}">{{ entity.id }}</a></td>
                            <td>{{ entity.name }}</td>
                            <td>{{ entity.description }}</td>
                            <td>{% if entity.createdAt %}{{ entity.createdAt|date('Y-m-d H:i:s') }}{% endif %}</td>
                            <td>
                                <ul>
                                    <li>
                                        <a href="{{ path('ticket_show', { 'ticket_id': entity.id, 'project_id': entity.project.id }) }}">show</a>
                                    </li>
                                    <li>
                                        <a href="{{ path('ticket_edit', { 'id': entity.id }) }}">edit</a>
                                    </li>
                                </ul>
                            </td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>

    {#<ul>#}
        {#<li>#}
            {#<a href="{{ path('ticket_new') }}">#}
                {#Create a new entry#}
            {#</a>#}
        {#</li>#}
    {#</ul>#}
{% endblock %}
