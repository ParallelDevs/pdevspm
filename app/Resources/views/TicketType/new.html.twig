{% extends '::base.html.twig' %}

{% block body -%}
    <h1>TicketType creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('tickettype') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
